-- Migration 001: Initial schema
-- This represents the baseline schema. All tables created via schema.sql already exist.
-- This migration file is here so that the migration system starts tracking from a known state.

-- Nothing to do: schema.sql uses CREATE TABLE IF NOT EXISTS.
SELECT 1;
